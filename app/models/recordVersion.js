var mongoose = require('mongoose');
/*
var extend = require('mongoose-schema-extend');
var Schema = mongoose.Schema;
var tax_objects = require('./taxonRecordName.js'); 
var taxonModel = tax_objects.TaxonRecordNameVersion;
var TaxonRecordNameVersion = require('mongoose').model(taxonModel).schema;

var recordVersion = new Schema({
	name : String,
	taxonRecordNameVersion : { type: Schema.Types.ObjectId, ref: 'TaxonRecordNameVersion' }
}, { strict: false, collection: 'RecordVersion' });




module.exports = mongoose.model('RecordVersion', recordVersion);
*/